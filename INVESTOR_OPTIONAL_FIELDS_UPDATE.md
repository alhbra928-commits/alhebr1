# ✅ تحديث: البريد الإلكتروني والهوية الوطنية اختيارية للمستثمرين

## المشكلة السابقة

كانت حقول البريد الإلكتروني والهوية الوطنية **إلزامية** عند تسجيل أو تعديل بيانات المستثمر:

```tsx
// التحقق القديم
if (!formData.email.trim()) {
  newErrors.email = 'البريد الإلكتروني مطلوب';  ❌
}

if (!formData.national_id.trim()) {
  newErrors.national_id = 'رقم الهوية مطلوب';  ❌
}
```

**المشاكل:**
- ❌ لا يمكن إضافة مستثمر بدون بريد إلكتروني
- ❌ لا يمكن إضافة مستثمر بدون هوية وطنية
- ❌ يجبر المدير على إدخال بيانات قد لا تكون متوفرة
- ❌ يؤخر عملية التسجيل السريع

---

## الحل المطبق

### 1. تعديل قاعدة البيانات ✅

قاعدة البيانات **جاهزة بالفعل** منذ Migration سابق:

```sql
-- الحقول nullable بالفعل
ALTER TABLE investors
ALTER COLUMN email DROP NOT NULL;

ALTER TABLE investors
ALTER COLUMN national_id DROP NOT NULL;

-- فهرس فريد يسمح بقيم فارغة متعددة
CREATE UNIQUE INDEX investors_unique_email_idx
ON investors (email)
WHERE email IS NOT NULL AND email != '';
```

**الفوائد:**
- ✅ يمكن حفظ بريد فارغ لأكثر من مستثمر
- ✅ يمكن حفظ هوية فارغة لأكثر من مستثمر
- ✅ البريد الحقيقي يبقى فريداً (لا تكرار)

---

### 2. تعديل نموذج المستثمرين (Frontend)

#### أ. تعديل Validation

**قبل:**
```tsx
// إلزامي - يمنع الحفظ إذا كان فارغاً
if (!formData.email.trim()) {
  newErrors.email = 'البريد الإلكتروني مطلوب';
}

if (!formData.national_id.trim()) {
  newErrors.national_id = 'رقم الهوية مطلوب';
}
```

**بعد:**
```tsx
// اختياري - يتحقق من الصيغة فقط إذا تم الإدخال
if (formData.email.trim() && !/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(formData.email)) {
  newErrors.email = 'البريد الإلكتروني غير صحيح';
}

if (formData.national_id.trim() && !/^\d{10}$/.test(formData.national_id)) {
  newErrors.national_id = 'رقم الهوية يجب أن يتكون من 10 أرقام';
}
```

**التغييرات:**
- ✅ إزالة التحقق من الفراغ
- ✅ التحقق من الصيغة فقط إذا تم إدخال قيمة
- ✅ السماح بالحفظ مع حقول فارغة

---

#### ب. تعديل Labels

**قبل:**
```tsx
البريد الإلكتروني *          ← علامة إلزامي
رقم الهوية الوطنية *         ← علامة إلزامي
```

**بعد:**
```tsx
<label className="flex items-center gap-2">
  <Mail className="h-4 w-4 text-[#C89B3C]" />
  البريد الإلكتروني
  <span className="text-xs text-gray-500 font-normal">(اختياري)</span>
</label>

<label className="flex items-center gap-2">
  <CreditCard className="h-4 w-4 text-[#C89B3C]" />
  رقم الهوية الوطنية
  <span className="text-xs text-gray-500 font-normal">(اختياري)</span>
</label>
```

**التغييرات:**
- ✅ إزالة علامة النجمة (*) الإلزامية
- ✅ إضافة نص "(اختياري)" باللون الرمادي
- ✅ وضوح للمستخدم أن الحقل اختياري

---

#### ج. تعديل Placeholder

**قبل:**
```tsx
placeholder="example@domain.com"
placeholder="1234567890"
```

**بعد:**
```tsx
placeholder="example@domain.com (اختياري)"
placeholder="1234567890 (اختياري)"
```

**الفائدة:**
- ✅ تذكير إضافي للمستخدم
- ✅ تجربة مستخدم أفضل

---

## النتيجة النهائية

### الحقول الإلزامية (فقط):
```
✅ الاسم الكامل *
✅ رقم الجوال *
```

### الحقول الاختيارية:
```
⭕ البريد الإلكتروني (اختياري)
⭕ رقم الهوية الوطنية (اختياري)
```

---

## السيناريوهات المدعومة

### 1. تسجيل سريع (اسم + جوال فقط)
```
الاسم: محمد أحمد
الجوال: 0501234567
البريد: [فارغ] ✅
الهوية: [فارغ] ✅
```

### 2. تسجيل كامل (كل البيانات)
```
الاسم: محمد أحمد
الجوال: 0501234567
البريد: m.ahmed@example.com ✅
الهوية: 1234567890 ✅
```

### 3. تسجيل جزئي
```
الاسم: محمد أحمد
الجوال: 0501234567
البريد: m.ahmed@example.com ✅
الهوية: [فارغ] ✅
```

---

## Validation الذكي

### البريد الإلكتروني

**إذا تم إدخاله:**
```
❌ "test" → خطأ: "البريد الإلكتروني غير صحيح"
❌ "test@" → خطأ: "البريد الإلكتروني غير صحيح"
✅ "test@example.com" → مقبول
```

**إذا كان فارغاً:**
```
✅ "" → مقبول (اختياري)
✅ تم حذف المحتوى → مقبول
```

### الهوية الوطنية

**إذا تم إدخالها:**
```
❌ "123" → خطأ: "رقم الهوية يجب أن يتكون من 10 أرقام"
❌ "12345678ab" → خطأ: "رقم الهوية يجب أن يتكون من 10 أرقام"
✅ "1234567890" → مقبول
```

**إذا كانت فارغة:**
```
✅ "" → مقبول (اختياري)
✅ تم حذف المحتوى → مقبول
```

---

## الملفات المعدلة

### Frontend:
```
src/modules/investors/components/InvestorFormModal.tsx
```

**التعديلات:**
1. ✅ تعديل دالة `validate()`
2. ✅ إزالة علامة * من Labels
3. ✅ إضافة نص "(اختياري)"
4. ✅ تحديث Placeholders

### Database:
```
قاعدة البيانات جاهزة - لا حاجة لتعديل
Migration: 20251022113927_make_investors_fields_nullable_for_public_booking.sql
```

---

## الفوائد

### للمدير:
```
✅ تسجيل أسرع للمستثمرين
✅ لا حاجة لبيانات غير متوفرة
✅ يمكن التحديث لاحقاً
✅ مرونة أكبر في الإدارة
```

### للمستثمر:
```
✅ تسجيل سريع بأقل بيانات
✅ خصوصية أفضل (بيانات أقل)
✅ يمكن إضافة البيانات لاحقاً
```

### للمنصة:
```
✅ تجربة مستخدم محسّنة
✅ عملية تسجيل مرنة
✅ معايير احترافية
✅ سهولة في الاستخدام
```

---

## أمثلة الاستخدام

### مثال 1: مستثمر بدون بريد

```tsx
// البيانات المدخلة
{
  full_name: "عبدالله محمد",
  phone: "0501234567",
  email: "",           // ← فارغ
  national_id: ""      // ← فارغ
}

// النتيجة
✅ يتم الحفظ بنجاح
✅ لا رسائل خطأ
```

### مثال 2: مستثمر ببريد فقط

```tsx
// البيانات المدخلة
{
  full_name: "عبدالله محمد",
  phone: "0501234567",
  email: "abdullah@example.com",  // ← موجود
  national_id: ""                  // ← فارغ
}

// النتيجة
✅ يتم الحفظ بنجاح
✅ البريد محفوظ
✅ الهوية فارغة (مسموح)
```

### مثال 3: بريد خاطئ

```tsx
// البيانات المدخلة
{
  full_name: "عبدالله محمد",
  phone: "0501234567",
  email: "abdullah",   // ← خاطئ
  national_id: ""
}

// النتيجة
❌ خطأ: "البريد الإلكتروني غير صحيح"
```

---

## القواعد النهائية

### الحقول الإلزامية
```
1. الاسم الكامل:
   • مطلوب دائماً
   • لا يمكن الحفظ بدونه

2. رقم الجوال:
   • مطلوب دائماً
   • صيغة: 05xxxxxxxx
   • 10 أرقام
   • يستخدم لتسجيل الدخول
```

### الحقول الاختيارية
```
1. البريد الإلكتروني:
   • اختياري
   • إذا تم إدخاله: يجب أن يكون بصيغة صحيحة
   • يمكن تركه فارغاً

2. رقم الهوية الوطنية:
   • اختياري
   • إذا تم إدخاله: يجب 10 أرقام
   • يمكن تركه فارغاً
```

---

## تحديثات لاحقة

يمكن للمدير أو المستثمر تحديث البيانات لاحقاً:

```
1. من لوحة إدارة المستثمرين:
   • فتح نموذج التعديل
   • إضافة/تعديل البريد
   • إضافة/تعديل الهوية
   • حفظ

2. من لوحة المستثمر (مستقبلاً):
   • تعديل الملف الشخصي
   • إضافة البيانات الناقصة
```

---

## الأمان والخصوصية

### حماية البيانات:
```
✅ RLS Policies فعّالة
✅ البريد الفريد محمي
✅ لا تكرار للبيانات الحقيقية
✅ القيم الفارغة غير محدودة
```

### Unique Constraints:
```sql
-- يسمح بـ:
✅ عدة مستثمرين ببريد فارغ
✅ عدة مستثمرين بهوية فارغة

-- يمنع:
❌ تكرار البريد الحقيقي
❌ تكرار رقم الجوال (constraint موجود)
```

---

## الخلاصة

**قبل التحديث:**
```
❌ البريد إلزامي
❌ الهوية إلزامية
❌ عملية تسجيل بطيئة
❌ بيانات قد لا تكون متوفرة
```

**بعد التحديث:**
```
✅ البريد اختياري
✅ الهوية اختيارية
✅ تسجيل سريع ومرن
✅ بيانات حسب التوفر
✅ يمكن التحديث لاحقاً
```

---

**الحقول الإلزامية فقط:**
- الاسم الكامل
- رقم الجوال

**الحقول الاختيارية:**
- البريد الإلكتروني
- رقم الهوية الوطنية

**النسخة:** v2025.12.16_011515
**Build:** ✅ نجح بدون أخطاء

---

**الآن يمكن إضافة وتعديل المستثمرين بدون الحاجة لإدخال البريد الإلكتروني أو الهوية الوطنية!** 🎉
