# 💰 النظام المالي الذكي للمزارع - التوثيق الكامل

## 📋 نظرة عامة

النظام المالي الذكي هو نظام متكامل يربط **المبلغ التسويقي** (من المستثمرين) بـ **المبلغ الفعلي** (لصاحب المزرعة) بشكل تلقائي، ويحسب الأرباح ويستقطع 25% لمحفظة الخير بدون أي تدخل يدوي.

---

## 🎯 المفاهيم الأساسية

### 1️⃣ المبلغ التسويقي (Marketing Amount)
- **المصدر**: إدارة المزارع → من حجوزات المستثمرين
- **التحديث**: تلقائي عند كل عملية شراء مكتملة
- **الوظيفة**: يمثل إجمالي الأموال المحصّلة من المستثمرين

### 2️⃣ المبلغ الفعلي (Actual Amount)
- **المصدر**: إدارة أصحاب المزارع → من بيانات المالك
- **التحديث**: يدوي (من نموذج صاحب المزرعة)
- **الوظيفة**: يمثل المبلغ المستحق لصاحب المزرعة

### 3️⃣ نسبة التغطية (Coverage Percentage)
```
نسبة التغطية = (المبلغ التسويقي ÷ المبلغ الفعلي) × 100
```

### 4️⃣ ربح المنصة (Platform Profit)
```
إذا كان: المبلغ التسويقي > المبلغ الفعلي
ربح المنصة = المبلغ التسويقي - المبلغ الفعلي
```

### 5️⃣ استقطاع الخير (Charity Amount)
```
استقطاع الخير = ربح المنصة × 25%
صافي ربح المنصة = ربح المنصة - استقطاع الخير
```

---

## 🔄 دورة العمل الكاملة

### المرحلة 1: جمع المبالغ التسويقية (Collecting)
- **الحالة**: نشطة (Active)
- **الوصف**: المزرعة تجمع الأموال من المستثمرين
- **الشرط**: نسبة التغطية < 100%

```sql
-- مثال
المبلغ التسويقي: 4,800 ريال
المبلغ الفعلي: 100,000 ريال
نسبة التغطية: 4.8%
المرحلة: collecting
```

### المرحلة 2: سداد صاحب المزرعة (Owner Payment)
- **المشغل**: تلقائي عند وصول التغطية 100%
- **الوصف**: تحويل المبلغ الفعلي كاملاً لصاحب المزرعة
- **التسجيل**: يُسجل في farm_financial_transactions

```sql
-- مثال
المبلغ التسويقي: 100,000 ريال
المبلغ الفعلي: 100,000 ريال
نسبة التغطية: 100%
المرحلة: owner_payment
✅ يتم تحويل 100,000 ريال للمالك
```

### المرحلة 3: استقطاع الخير (Charity Deduction)
- **الشرط**: إذا كان هناك ربح (التسويقي > الفعلي)
- **الوصف**: استقطاع 25% من الربح لمحفظة الخير
- **التسجيل**: charity_deductions_log

```sql
-- مثال
المبلغ التسويقي: 150,000 ريال
المبلغ الفعلي: 100,000 ريال
ربح المنصة: 50,000 ريال
استقطاع الخير (25%): 12,500 ريال
صافي ربح المنصة: 37,500 ريال
```

### المرحلة 4: الإكمال (Completed)
- **الحالة**: مكتملة (Completed)
- **الوصف**: تم سداد المالك + استقطاع الخير + تسجيل الأرباح
- **النتيجة**: المزرعة جاهزة للانتقال للخدمات الزراعية

---

## 🗄️ بنية قاعدة البيانات

### الجداول الرئيسية

#### 1. `smart_farm_finances`
البطاقة المالية الذكية لكل مزرعة

| الحقل | النوع | الوصف |
|------|------|-------|
| `farm_code` | TEXT | كود المزرعة (FARM-2025-0003) |
| `farm_name` | TEXT | اسم المزرعة |
| `owner_name` | TEXT | اسم المالك |
| `marketing_amount` | NUMERIC | المبلغ التسويقي (تلقائي) |
| `actual_amount` | NUMERIC | المبلغ الفعلي (من المالك) |
| `coverage_percentage` | NUMERIC | نسبة التغطية (تلقائي) |
| `platform_profit` | NUMERIC | ربح المنصة (تلقائي) |
| `charity_amount` | NUMERIC | استقطاع الخير (تلقائي) |
| `completion_stage` | TEXT | المرحلة الحالية |
| `financial_barcode` | TEXT | الباركود المالي |

#### 2. `farm_financial_transactions`
سجل جميع المعاملات المالية

| الحقل | النوع | الوصف |
|------|------|-------|
| `transaction_type` | TEXT | نوع المعاملة |
| `amount` | NUMERIC | المبلغ |
| `description` | TEXT | الوصف |
| `investor_name` | TEXT | اسم المستثمر (إن وجد) |

**أنواع المعاملات:**
- `marketing_income` - دخل تسويقي
- `owner_payment` - دفع للمالك
- `platform_profit` - ربح المنصة
- `charity_deduction` - استقطاع الخير

#### 3. `platform_charity_wallet`
محفظة الخير (25% من الأرباح)

| الحقل | النوع | الوصف |
|------|------|-------|
| `total_balance` | NUMERIC | الرصيد الحالي |
| `total_received` | NUMERIC | إجمالي المستلم |
| `farms_contributed` | INTEGER | عدد المزارع المساهمة |

#### 4. `charity_deductions_log`
سجل استقطاعات الخير

| الحقل | النوع | الوصف |
|------|------|-------|
| `farm_code` | TEXT | كود المزرعة |
| `platform_profit` | NUMERIC | ربح المنصة |
| `charity_amount` | NUMERIC | المبلغ المستقطع |
| `deduction_date` | TIMESTAMPTZ | تاريخ الاستقطاع |

---

## ⚙️ الدوال التلقائية (Triggers)

### 1. `auto_update_marketing_amount()`
**المشغل**: عند إضافة/تحديث حجز

```sql
-- يحدث عند
INSERT INTO reservations ...
UPDATE reservations SET payment_status = 'completed' ...

-- يقوم بـ
1. حساب إجمالي المبلغ التسويقي من جميع الحجوزات
2. تحديث smart_farm_finances.marketing_amount
3. تسجيل معاملة في farm_financial_transactions
```

### 2. `auto_calculate_farm_finances()`
**المشغل**: عند تحديث المبلغ التسويقي أو الفعلي

```sql
-- يحسب
- نسبة التغطية
- المتبقي
- ربح المنصة (إن وجد)
- استقطاع الخير 25%
- صافي ربح المنصة

-- عند وصول التغطية 100%
1. يسدد المبلغ الفعلي للمالك
2. يستقطع 25% للخير
3. يسجل جميع المعاملات
4. يحدث محفظة الخير
```

### 3. `auto_create_farm_finance_card()`
**المشغل**: عند إضافة مزرعة جديدة

```sql
-- يقوم بـ
1. جلب المبلغ الفعلي من farm_owners
2. توليد باركود مالي فريد
3. إنشاء بطاقة مالية في smart_farm_finances
```

---

## 🎨 واجهة المستخدم

### البطاقة المالية ثلاثية الأبعاد

#### الوجه الأمامي
```
┌────────────────────────────────────┐
│  🌳 مزرعة الخالدية                 │
│  FARM-2025-0003                   │
│  المالك: راشد سعد                 │
│                                    │
│  ┌──────────────────────────────┐ │
│  │  نسبة التغطية                │ │
│  │  ███████░░░░░░ 4.8%          │ │
│  │  💰 جمع المبالغ التسويقية    │ │
│  └──────────────────────────────┘ │
│                                    │
│  📈 التسويقي    💰 الفعلي        │
│  4,800 ر.س      100,000 ر.س     │
│                                    │
│  👥 1 مستثمر    🌳 4 أشجار       │
└────────────────────────────────────┘
```

#### الوجه الخلفي (الباركود)
```
┌────────────────────────────────────┐
│  البطاقة المالية                  │
│                                    │
│  ┌──────────────────────────────┐ │
│  │ ║║ ║ ║║║  ║ ║║ ║║║ ║ ║║    │ │
│  │ ║║ ║ ║║║  ║ ║║ ║║║ ║ ║║    │ │
│  │ FIN-FARM-2025-0003-A1B2C3   │ │
│  └──────────────────────────────┘ │
│                                    │
│  📊 إجمالي المعاملات: 1          │
│  📅 آخر معاملة: 2025-10-23       │
└────────────────────────────────────┘
```

### الإحصائيات الرئيسية

```
┌─────────────────┬─────────────────┬─────────────────┬─────────────────┐
│ 📈 التسويقي    │ 💰 الفعلي      │ 📊 الربح       │ 💚 الخير       │
│ 4,800 ر.س      │ 100,000 ر.س    │ 0 ر.س          │ 0 ر.س          │
├─────────────────┴─────────────────┴─────────────────┴─────────────────┤
│ 🏢 مزارع نشطة: 1  │  👥 مستثمرين: 1  │  🌳 أشجار: 4  │  📊 متوسط: 4.8% │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 🚀 كيفية الاستخدام

### 1. إضافة مزرعة جديدة
```typescript
// في إدارة أصحاب المزارع
1. أدخل بيانات المالك
2. أدخل المبلغ الفعلي (actual_price)
3. احفظ

// تلقائياً
✅ يتم إنشاء بطاقة مالية ذكية
✅ يتم ربط المبلغ الفعلي
✅ يتم توليد باركود مالي
```

### 2. إضافة حجز جديد
```typescript
// في إدارة الحجوزات
1. اختر المزرعة
2. أدخل بيانات المستثمر
3. أدخل عدد الأشجار
4. اكتمل الدفع

// تلقائياً
✅ يُضاف المبلغ للمبلغ التسويقي
✅ تُحدث نسبة التغطية
✅ تُسجل المعاملة
```

### 3. عرض البطاقة المالية
```typescript
// في الإدارة المالية
1. افتح "💰 النظام المالي الذكي"
2. اعرض بطاقات المزارع
3. اضغط على البطاقة للتفاصيل
4. اقلب البطاقة لرؤية الباركود
```

---

## 📊 أمثلة واقعية

### مثال 1: مزرعة في مرحلة الجمع
```
المزرعة: مزرعة الخالدية
المبلغ التسويقي: 4,800 ريال
المبلغ الفعلي: 100,000 ريال
نسبة التغطية: 4.8%
المرحلة: جمع المبالغ التسويقية
الحالة: نشطة
```

### مثال 2: مزرعة وصلت للتغطية الكاملة
```
المزرعة: مزرعة النخيل
المبلغ التسويقي: 100,000 ريال
المبلغ الفعلي: 100,000 ريال
نسبة التغطية: 100%
المرحلة: سداد المالك

✅ العمليات التلقائية:
1. تحويل 100,000 ريال للمالك
2. لا يوجد ربح (0 ريال)
3. لا يوجد استقطاع خير
4. الحالة: مكتملة
```

### مثال 3: مزرعة بربح وخير
```
المزرعة: مزرعة الزيتون
المبلغ التسويقي: 150,000 ريال
المبلغ الفعلي: 100,000 ريال
نسبة التغطية: 150%
ربح المنصة: 50,000 ريال
المرحلة: استقطاع الخير

✅ العمليات التلقائية:
1. تحويل 100,000 ريال للمالك ✅
2. استقطاع الخير: 12,500 ريال (25%) ✅
3. صافي ربح المنصة: 37,500 ريال ✅
4. تحديث محفظة الخير ✅
5. الحالة: مكتملة
```

---

## 🔐 الأمان (RLS Policies)

```sql
-- القراءة: الجميع
smart_farm_finances → SELECT → public

-- الكتابة: authenticated فقط
smart_farm_finances → INSERT/UPDATE/DELETE → authenticated

-- محفظة الخير: قراءة للجميع، تحديث للمصادقين فقط
platform_charity_wallet → SELECT → public
platform_charity_wallet → UPDATE → authenticated
```

---

## 🎯 الميزات الذكية

### ✅ التحديث التلقائي
- كل حجز يُحدث المبلغ التسويقي تلقائياً
- نسبة التغطية تُحسب لحظياً
- الأرباح واستقطاع الخير تلقائي 100%

### ✅ لا تدخل يدوي
- لا حاجة لإدخال المبالغ يدوياً
- لا حاجة لحساب النسب
- لا حاجة لاستقطاع الخير يدوياً

### ✅ الشفافية الكاملة
- كل معاملة مسجلة
- سجل كامل للخير
- باركود لكل بطاقة مالية

### ✅ الأداء العالي
- Indexes على جميع الحقول المهمة
- Triggers محسّنة
- استعلامات سريعة

---

## 🎨 الألوان والهوية البصرية

### نسبة التغطية
- 🔴 0-25%: أحمر
- 🟠 25-50%: برتقالي
- 🟡 50-75%: أصفر
- 🟢 75-100%: أخضر فاتح
- ✅ 100%+: أخضر

### الحالات
- 🔵 Active: أزرق
- 🟡 In Progress: أصفر
- 🟢 Completed: أخضر
- ⚫ Closed: رمادي

---

## 📝 ملاحظات مهمة

1. **المبلغ الفعلي من صاحب المزرعة**: تأكد من إدخاله بدقة
2. **التحديث التلقائي**: لا تعدل المبالغ يدوياً
3. **الباركود**: فريد لكل مزرعة ويظهر خلف البطاقة
4. **محفظة الخير**: تُحدث تلقائياً عند كل ربح

---

**تم التطوير بواسطة**: النظام المالي الذكي v1.0
**التاريخ**: 2025-10-23
**الحالة**: ✅ جاهز للإنتاج
