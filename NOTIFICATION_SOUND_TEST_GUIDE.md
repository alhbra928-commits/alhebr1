# 🧪 دليل اختبار نظام الإشعارات الصوتية

## 📋 نظرة عامة

هذا الدليل يشرح كيفية اختبار نظام الإشعارات الصوتية بالكامل للتأكد من عمله بشكل صحيح.

---

## 🎯 طرق الاختبار

### الطريقة 1️⃣: اختبار من لوحة التحكم (الطريقة الرئيسية)

#### الخطوات:

1. **تسجيل الدخول كمشرف:**
   ```
   - افتح المتصفح واذهب للموقع
   - اضغط على أيقونة التاج 👑 في المنصة العامة
   - سجل دخول كمشرف
   ```

2. **تفعيل الإشعارات:**
   ```
   - ستجد زر دائري في أسفل اليسار 🔊
   - لونه رمادي (متوقف) في البداية
   - اضغط عليه لفتح نافذة الإعدادات
   ```

3. **تفعيل المراقبة:**
   ```
   - في نافذة الإعدادات
   - فعّل "مراقبة الحجوزات المباشرة"
   - اضغط "السماح" عندما يطلب المتصفح الإذن
   - الزر سيتحول للأخضر ويبدأ النبض 🟢
   ```

4. **اختبار الأصوات:**
   ```
   - في نفس النافذة، جرّب كل صوت بزر ▶
   - 🔔 حجز جديد: نغمة ثلاثية صاعدة
   - 💰 دفعة مستلمة: كاشينج
   - ⚠️ تنبيه عاجل: صوت متكرر
   - ✅ نجاح: تأكيد قصير
   ```

5. **اختبار حجز حقيقي:**
   ```
   - افتح تبويب جديد في نفس المتصفح
   - اذهب للمنصة العامة
   - اختر مزرعة واعمل حجز جديد
   - ارجع للوحة التحكم
   - يجب أن تسمع الصوت تلقائياً! 🔔
   ```

---

### الطريقة 2️⃣: صفحة الاختبار المخصصة

#### الخطوات:

1. **افتح صفحة الاختبار:**
   ```
   افتح في المتصفح:
   http://localhost:5173/test-notification-sounds.html
   ```

2. **اختبار الأصوات:**
   ```
   - اضغط على أي من البطاقات الأربع
   - يجب أن تسمع الصوت فوراً
   - تحقق من سجل الأحداث في الأسفل
   ```

3. **اختبار المراقبة:**
   ```
   - اضغط "بدء المراقبة"
   - الدائرة ستتحول للأخضر وتنبض
   - اضغط "السماح" للإشعارات
   ```

4. **محاكاة حجز:**
   ```
   - اضغط "إنشاء حجز تجريبي"
   - سيتم إدخال حجز في قاعدة البيانات
   - يجب أن يشتغل الصوت تلقائياً! 🎉
   - تحقق من سجل الأحداث
   ```

---

## ✅ قائمة التحقق (Checklist)

### اختبار الأصوات الأساسية:

- [ ] صوت حجز جديد يشتغل (C5→E5→G5)
- [ ] صوت دفعة مستلمة يشتغل (كاشينج)
- [ ] صوت تنبيه عاجل يشتغل (متكرر 3 مرات)
- [ ] صوت نجاح يشتغل (تأكيد قصير)

### اختبار واجهة المستخدم:

- [ ] الزر العائم يظهر في أسفل اليسار
- [ ] الزر رمادي عند الإيقاف
- [ ] الزر أخضر + pulse عند التفعيل
- [ ] نافذة الإعدادات تفتح بالضغط
- [ ] 4 بطاقات للأصوات ظاهرة
- [ ] Toggle switches تعمل
- [ ] Sliders مستوى الصوت تعمل
- [ ] أزرار التجربة (▶) تعمل

### اختبار المراقبة:

- [ ] زر "بدء المراقبة" يعمل
- [ ] طلب إذن الإشعارات يظهر
- [ ] حالة المراقبة تتغير (نشط)
- [ ] الاتصال بـ Supabase ينجح
- [ ] Channel subscription ناجح

### اختبار الحجز الحقيقي:

- [ ] إنشاء حجز جديد من المنصة
- [ ] الصوت يشتغل تلقائياً
- [ ] إشعار المتصفح يظهر
- [ ] التوقيت صحيح (أقل من 10 ثواني)

### اختبار المتصفحات:

- [ ] Chrome/Edge (Chromium)
- [ ] Firefox
- [ ] Safari (إن أمكن)

---

## 🔍 المشاكل الشائعة وحلولها

### 1. الصوت لا يشتغل:

**الأعراض:**
- الأزرار لا تصدر صوت
- لا توجد أخطاء في Console

**الحلول:**
```
✓ تأكد من أن مستوى الصوت في النظام مفعّل
✓ تأكد من أن المتصفح غير مكتوم
✓ جرّب الضغط على الصفحة أولاً (بعض المتصفحات تحتاج تفاعل)
✓ افتح Console وابحث عن أخطاء AudioContext
```

### 2. المراقبة لا تبدأ:

**الأعراض:**
- زر "بدء المراقبة" لا يعمل
- الحالة تبقى "متوقف"

**الحلول:**
```
✓ افتح Console وابحث عن أخطاء Supabase
✓ تحقق من اتصال الإنترنت
✓ تأكد من صحة SUPABASE_URL و SUPABASE_KEY
✓ تحقق من RLS policies لجدول reservations
```

### 3. إشعارات المتصفح لا تظهر:

**الأعراض:**
- الصوت يشتغل لكن لا إشعار

**الحلول:**
```
✓ تحقق من أنك ضغطت "السماح" للإشعارات
✓ افتح إعدادات المتصفح → الإشعارات
✓ تأكد من أن الموقع مسموح له
✓ جرّب في وضع Incognito
```

### 4. الصوت يشتغل للحجوزات القديمة:

**الأعراض:**
- عند فتح الصفحة، تشتغل أصوات كثيرة

**الحلول:**
```
✓ هذا طبيعي إذا كانت هناك حجوزات حديثة
✓ النظام يفلتر الحجوزات الأقدم من 10 ثواني
✓ يمكنك تعديل الفلتر في handleNewBooking()
```

---

## 🧪 اختبارات متقدمة

### اختبار 1: أداء النظام

```javascript
// في Console المتصفح:

// 1. اختبار تكرار الصوت
for (let i = 0; i < 5; i++) {
  setTimeout(() => {
    notificationSoundService.playNewBookingSound();
  }, i * 1000);
}

// 2. اختبار تغيير مستوى الصوت
notificationSoundService.setVolume('new_booking', 0.3);
notificationSoundService.playNewBookingSound();

// 3. اختبار التفعيل/الإيقاف
notificationSoundService.enableSound('new_booking', false);
notificationSoundService.playNewBookingSound(); // لن يشتغل
notificationSoundService.enableSound('new_booking', true);
notificationSoundService.playNewBookingSound(); // سيشتغل
```

### اختبار 2: قاعدة البيانات

```sql
-- في Supabase SQL Editor:

-- 1. إدخال حجز تجريبي
INSERT INTO reservations (
  customer_name,
  customer_phone,
  reserved_trees,
  total_amount,
  status,
  booking_status
) VALUES (
  'مستثمر اختبار',
  '0500000000',
  5,
  500,
  'pending',
  'pending'
);

-- 2. التحقق من الحجوزات الحديثة
SELECT
  id,
  customer_name,
  created_at,
  EXTRACT(EPOCH FROM (NOW() - created_at)) as seconds_ago
FROM reservations
WHERE created_at > NOW() - INTERVAL '1 minute'
ORDER BY created_at DESC;
```

### اختبار 3: Realtime Subscription

```javascript
// في Console المتصفح:

// 1. التحقق من حالة الاشتراك
console.log('Is monitoring:', bookingNotificationMonitor.isActive());

// 2. الاشتراك يدوياً
const testChannel = supabase
  .channel('test_channel')
  .on('postgres_changes',
    { event: 'INSERT', schema: 'public', table: 'reservations' },
    (payload) => console.log('Received:', payload)
  )
  .subscribe((status) => console.log('Status:', status));

// 3. إلغاء الاشتراك
supabase.removeChannel(testChannel);
```

---

## 📊 النتائج المتوقعة

### عند التشغيل الناجح:

```
✅ AudioContext initialized
✅ Supabase connected
✅ Channel subscribed
✅ Notification permission granted
✅ Sound plays on new booking
✅ Browser notification appears
✅ Event logged correctly
```

### في Console المتصفح:

```
Starting booking notification monitor...
Subscription status: CHANNEL_STATE_JOINED
Subscription status: SUBSCRIBED
New booking detected: {new: {...}}
```

### في صفحة الاختبار:

```
🚀 بدء اختبار النظام...
✅ AudioContext جاهز
✅ الاتصال بقاعدة البيانات ناجح
🔔 حالة إذن الإشعارات: granted
📡 بدء المراقبة المباشرة...
📡 حالة الاتصال: SUBSCRIBED
⚡ جاري إنشاء حجز تجريبي...
✅ تم إنشاء الحجز التجريبي بنجاح!
🎉 حجز جديد اكتُشف!
🔔 تشغيل صوت حجز جديد
```

---

## 🎬 سيناريو اختبار كامل

### السيناريو الموصى به:

```
1. افتح المتصفح (Chrome مفضل)

2. سجل دخول كمشرف في لوحة التحكم

3. اضغط على زر الإشعارات في أسفل اليسار

4. جرّب كل صوت (▶) للتأكد من عمله:
   - حجز جديد
   - دفعة مستلمة
   - تنبيه عاجل
   - نجاح

5. فعّل "مراقبة الحجوزات المباشرة"

6. اسمح بالإشعارات عندما يطلب المتصفح

7. تحقق من:
   - الزر أصبح أخضر
   - يوجد نبض (pulse)
   - الحالة "نشط - جاري المراقبة"

8. افتح تبويب جديد

9. اذهب للمنصة العامة

10. اختر مزرعة واعمل حجز:
    - ادخل البيانات
    - اضغط "حجز الآن"

11. ارجع للوحة التحكم

12. يجب أن تسمع: 🔔 صوت الحجز
    ويظهر: إشعار المتصفح

13. ✅ النظام يعمل بنجاح!
```

---

## 📞 الدعم

إذا واجهت مشاكل:

1. افتح Console (F12)
2. ابحث عن أخطاء حمراء
3. اضغط Network → WS لرؤية WebSocket
4. تحقق من Supabase Dashboard → API logs

---

## 🎉 تهانينا!

إذا نجحت جميع الاختبارات، النظام يعمل بشكل مثالي!

**الآن يمكنك:**
- مراقبة الحجوزات مباشرة
- الحصول على إشعارات فورية
- التحكم في الأصوات حسب تفضيلاتك
- العمل براحة بال 😊

---

## 📝 ملاحظات إضافية

### للمطورين:

```typescript
// الملفات المهمة:
- src/services/notificationSoundService.ts (400+ سطر)
- src/components/common/NotificationSoundControl.tsx (300+ سطر)
- src/modules/dashboard/EnhancedDashboard.tsx (تكامل)

// الـ API المستخدمة:
- Web Audio API (توليد الأصوات)
- Notification API (إشعارات المتصفح)
- Supabase Realtime (المراقبة)
- React Hooks (الحالة)

// الجداول:
- reservations (المراقبة)
- RLS policies (الأمان)
```

### للمشرفين:

```
✓ النظام يعمل تلقائياً
✓ لا يحتاج صيانة
✓ يمكن التفعيل/الإيقاف بأي وقت
✓ الإعدادات تحفظ في الجلسة
✓ آمن وخصوصي بالكامل
```

---

**تاريخ الإصدار:** 2025-10-24
**الإصدار:** 1.0.0
**الحالة:** ✅ جاهز للإنتاج
