// Notification Sound Service

export const bookingNotificationMonitor = {
  start: () => {},
  stop: () => {}
};

export const notificationSoundService = {
  playSound: () => {
    // Play notification sound
  },

  isEnabled: () => {
    return true;
  },

  toggle: () => {
    // Toggle sound
  }
};
