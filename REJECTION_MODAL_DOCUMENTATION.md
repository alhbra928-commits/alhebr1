# 📋 نافذة رفض الطلبات - دليل شامل

## ✨ نظرة عامة

تم إضافة نافذة منبثقة احترافية لرفض طلبات أصحاب المزارع مع أسباب محددة مسبقاً وخيار إدخال سبب مخصص.

---

## 🎯 الميزات الرئيسية

### 1️⃣ **تصميم احترافي**
- نافذة منبثقة جميلة مع تدرجات لونية حمراء
- رأس مميز مع أيقونة وزر إغلاق
- تأثيرات حركية عند التفاعل

### 2️⃣ **9 أسباب رفض جاهزة**
```
✅ معلومات غير كاملة أو ناقصة
✅ بيانات غير صحيحة أو مزورة
✅ عدم توافق المزرعة مع الشروط المطلوبة
✅ المستندات المرفقة غير واضحة
✅ تكرار الطلب
✅ موقع المزرعة غير مناسب
✅ عدد الأشجار غير مطابق للواقع
✅ السعر المطلوب غير معقول
✅ سبب آخر (أدخل تفاصيل أدناه)
```

### 3️⃣ **خيار "سبب آخر"**
- عند اختيار "سبب آخر" يظهر حقل نصي
- يسمح بإدخال سبب مخصص مفصل
- تحقق من عدم ترك الحقل فارغاً

### 4️⃣ **التحقق الذكي**
- ⚠️ لا يمكن الرفض بدون اختيار سبب
- ⚠️ إذا اخترت "سبب آخر" يجب كتابة التفاصيل
- زر "تأكيد الرفض" معطّل حتى اختيار سبب

### 5️⃣ **الحذف التلقائي**
- بعد الرفض، الطلب يختفي فوراً من القائمة
- تحديث `status` إلى `rejected`
- Soft Delete - البيانات محفوظة للأرشفة

### 6️⃣ **الإشعارات التلقائية**
- يُرسل إشعار للمالك فوراً
- يتضمن سبب الرفض بالتفصيل
- يشجع المالك على إعادة الإرسال بعد التصحيح

---

## 🔧 كيفية الاستخدام

### الخطوات:

1. **افتح لوحة الإدارة**
   ```
   انتقل إلى: إدارة أصحاب المزارع
   ```

2. **ابحث عن الطلبات المعلقة**
   ```
   ستجد قسم: "🕐 طلبات المراجعة المعلقة (X)"
   ```

3. **اضغط على زر "رفض"**
   - ستظهر نافذة منبثقة جميلة
   - تعرض اسم المالك ورقم جواله

4. **اختر سبب الرفض**
   - انقر على أحد الأسباب التسعة
   - سيتم تظليل السبب المختار باللون الأحمر

5. **إذا اخترت "سبب آخر"**
   - سيظهر حقل نصي أسفل القائمة
   - اكتب سبب الرفض المخصص بالتفصيل

6. **اضغط "تأكيد الرفض"**
   - سيُرسل الطلب للنظام
   - سيُحذف من القائمة
   - سيُرسل إشعار للمالك

7. **إلغاء العملية**
   - اضغط زر "إلغاء" أو زر ❌ في الأعلى
   - النافذة ستُغلق بدون حفظ

---

## 📊 الهيكل التقني

### State Variables المضافة:
```typescript
const [showRejectModal, setShowRejectModal] = useState(false);
const [submissionToReject, setSubmissionToReject] = useState<any>(null);
const [selectedRejectionReason, setSelectedRejectionReason] = useState('');
const [customRejectionReason, setCustomRejectionReason] = useState('');
```

### الدالة المستخدمة:
```typescript
OwnersService.rejectSubmission(submissionId, finalReason)
```

### دالة قاعدة البيانات:
```sql
SELECT reject_farm_submission(
  p_submission_id := 'uuid',
  p_admin_id := 'uuid',
  p_rejection_reason := 'text'
);
```

---

## 🗄️ التغييرات في قاعدة البيانات

عند الرفض، يتم:

### 1. تحديث `farm_submission_requests`
```sql
UPDATE farm_submission_requests
SET
  status = 'rejected',
  reviewed_at = now(),
  reviewed_by = p_admin_id,
  rejection_reason = p_rejection_reason
WHERE id = p_submission_id;
```

### 2. تحديث `farm_owner_profiles`
```sql
UPDATE farm_owner_profiles
SET
  status = 'rejected',
  rejected_at = now(),
  rejected_by = p_admin_id,
  rejection_reason = p_rejection_reason
WHERE id = v_profile_id;
```

### 3. إنشاء إشعار
```sql
INSERT INTO farm_owner_notifications (
  profile_id,
  title_ar,
  message_ar,
  notification_type,
  priority
) VALUES (
  v_profile_id,
  '🔴 تم رفض طلب المزرعة',
  'عذراً، تم رفض طلب مزرعتك. السبب: ...',
  'submission_rejected',
  'high'
);
```

---

## 🎨 التصميم

### الألوان المستخدمة:
- **الرأس:** `from-red-500 to-red-600`
- **السبب المختار:** `bg-red-50 border-red-500`
- **السبب العادي:** `bg-gray-50 border-gray-200`
- **زر التأكيد:** `from-red-500 to-red-600`
- **زر الإلغاء:** `bg-gray-200`

### التأثيرات:
- ✨ Backdrop blur على الخلفية
- ✨ Shadow كبير للنافذة
- ✨ Hover effects على الأسباب
- ✨ Transform على زر التأكيد
- ✨ Transition سلسة على كل العناصر

---

## ✅ اختبار الميزة

### السيناريو 1: رفض برمضى محدد مسبقاً
1. افتح نافذة الرفض
2. اختر "معلومات غير كاملة أو ناقصة"
3. اضغط "تأكيد الرفض"
4. **النتيجة المتوقعة:** الطلب يختفي والمالك يستلم إشعاراً

### السيناريو 2: رفض بسبب مخصص
1. افتح نافذة الرفض
2. اختر "سبب آخر (أدخل تفاصيل أدناه)"
3. اكتب: "المزرعة في منطقة غير مخدومة حالياً"
4. اضغط "تأكيد الرفض"
5. **النتيجة المتوقعة:** الطلب يُرفض مع السبب المخصص

### السيناريو 3: محاولة رفض بدون سبب
1. افتح نافذة الرفض
2. لا تختر أي سبب
3. **النتيجة المتوقعة:** زر "تأكيد الرفض" معطّل

### السيناريو 4: إلغاء العملية
1. افتح نافذة الرفض
2. اختر سبباً
3. اضغط "إلغاء"
4. **النتيجة المتوقعة:** النافذة تُغلق بدون حفظ

---

## 🐛 استكشاف الأخطاء

### خطأ: "الرجاء اختيار سبب الرفض"
**السبب:** لم يتم اختيار أي سبب
**الحل:** اختر سبباً من القائمة

### خطأ: "الرجاء إدخال تفاصيل سبب الرفض"
**السبب:** اخترت "سبب آخر" لكن لم تكتب التفاصيل
**الحل:** اكتب سبب الرفض في الحقل النصي

### خطأ: "حدث خطأ أثناء رفض الطلب"
**السبب:** مشكلة في الاتصال أو الدالة
**الحل:**
1. تحقق من Console
2. تأكد من وجود `reject_farm_submission`
3. تحقق من صلاحيات قاعدة البيانات

---

## 📝 ملاحظات مهمة

1. ✅ **Soft Delete:** البيانات محفوظة في قاعدة البيانات
2. ✅ **الإشعارات:** تُرسل تلقائياً مع سبب الرفض
3. ✅ **الأرشفة:** يمكن استرجاع الطلبات المرفوضة
4. ✅ **Audit Log:** كل عملية رفض مسجلة مع التفاصيل
5. ✅ **إعادة المحاولة:** المالك يمكنه إعادة الإرسال

---

## 🎉 الحالة النهائية

```bash
✓ النافذة المنبثقة جاهزة
✓ 9 أسباب رفض محددة مسبقاً
✓ خيار سبب مخصص
✓ التحقق الذكي من المدخلات
✓ الحذف التلقائي من القائمة
✓ الإشعارات التلقائية
✓ البناء نجح بدون أخطاء
```

**كل شيء جاهز للاستخدام!** 🚀
