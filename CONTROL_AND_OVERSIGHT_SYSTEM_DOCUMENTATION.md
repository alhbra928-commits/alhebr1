# 🛡️ نظام الرقابة والصلاحيات المتكامل - منصة النخيل والزيتون

## 📋 نظرة عامة

نظام رقابة مركزي ذكي مرتبط بجميع أقسام لوحة الإدارة، يرصد ويسجل ويحلل كل إجراء في الوقت الفعلي، مع تمكين المدير العام من الإدارة اللحظية الكاملة.

---

## 🎯 **الأقسام الرئيسية الخمسة**

### **1️⃣ المستخدمون الإداريون (Admin Users Manager)**

#### **الوظيفة:**
```
إدارة شاملة لجميع المدراء والموظفين المصرح لهم بالدخول إلى لوحة الإدارة
```

#### **المميزات:**
- ✅ عرض جميع المستخدمين في بطاقات ثلاثية الأبعاد
- ✅ حالة كل مستخدم (نشط / مجمّد / غير متصل)
- ✅ آخر دخول لكل مستخدم
- ✅ المسمى الوظيفي
- ✅ إحصائيات سريعة (إجمالي / نشط / مجمّد)

#### **الأزرار المتاحة:**
```typescript
✏️ تعديل الصلاحيات - فتح واجهة تعديل الصلاحيات
🔒 تجميد - تجميد فوري للمستخدم
✅ تفعيل - تفعيل مستخدم مجمّد
🗑️ حذف - حذف نهائي (مع تأكيد)
➕ إضافة مستخدم - إضافة مستخدم جديد
```

#### **التصميم:**
- **الخلفية**: Gradient داكن (زيتوني × رمادي)
- **البطاقات**: شفافة مع حواف ملونة حسب الحالة
- **التأثيرات**: Hover scale + Shadow

#### **السجل:**
```typescript
// جميع العمليات تُسجل تلقائياً
'freeze_user' - تجميد مستخدم
'activate_user' - تفعيل مستخدم
'delete_user' - حذف مستخدم
'update_permissions' - تحديث صلاحيات
```

---

### **2️⃣ الجلسات الحية (Live Sessions Monitor)**

#### **الوظيفة:**
```
مراقبة لحظية لجميع الجلسات النشطة مع إمكانية الإنهاء الفوري
```

#### **البيانات المعروضة:**
```typescript
interface LiveSession {
  admin_name: string;          // اسم المستخدم
  admin_phone: string;         // رقم الجوال
  current_module: string;      // القسم الحالي
  duration: number;            // مدة الجلسة بالدقائق
  last_activity: string;       // آخر نشاط
  status: 'active' | 'idle';   // الحالة
  device_info: string;         // نوع الجهاز
  ip_address: string;          // عنوان IP
}
```

#### **المؤشرات الذكية:**
```
🟢 نشط - آخر نشاط أقل من 20 دقيقة
🟡 خامل - آخر نشاط أكثر من 20 دقيقة
```

#### **الإجراءات المتاحة:**
```
👁️ عرض التفاصيل - نافذة modal مع كل البيانات
🛑 إنهاء الجلسة - فصل فوري + تسجيل في السجل
```

#### **التحديث التلقائي:**
- تحديث كل 10 ثوان
- Realtime monitoring بدون إعادة تحميل

#### **التنبيهات:**
```
⚠️ عند وجود جلسة خاملة:
"تحذير: هذه الجلسة خاملة منذ أكثر من 20 دقيقة"
```

---

### **3️⃣ سجل الإجراءات (Action Logs Viewer)**

#### **الوظيفة:**
```
الذاكرة التاريخية الكاملة - كل إجراء يتم في أي قسم يُسجل هنا
```

#### **البيانات المسجلة:**
```typescript
interface ActionLog {
  id: string;                  // رمز تتبع فريد
  admin_name: string;          // المنفذ
  admin_phone: string;         // رقم جوال المنفذ
  action_type: string;         // نوع الإجراء
  action_details: string;      // تفاصيل كاملة
  device_info: string;         // معلومات الجهاز
  ip_address: string;          // عنوان IP
  action_status: string;       // ناجح / فشل
  created_at: string;          // التوقيت الدقيق
}
```

#### **أنواع الإجراءات المسجلة:**
```
📥 تسجيل دخول (login)
📤 خروج (logout)
🛑 إنهاء جلسة (session_terminated)
🌍 استكشاف المنصة (explore_platform)
🔒 تجميد مستخدم (freeze_user)
✅ تفعيل مستخدم (activate_user)
🗑️ حذف مستخدم (delete_user)
⚙️ تحديث صلاحيات (update_permissions)
🎯 إنهاء من الرقابة (terminate_session_from_control)
➕ إنشاء (create)
✏️ تعديل (update/edit)
❌ حذف (delete)
✔️ اعتماد (approve)
💸 تحويل (transfer)
```

#### **البحث والتصفية:**
```typescript
// بحث متقدم
- البحث في: الاسم، الجوال، التفاصيل
- تصفية بالحالة: جميع / ناجح / فشل
- تصفية بالنوع: 15+ نوع إجراء

// إحصائيات سريعة
- إجمالي السجلات
- عدد العمليات الناجحة
- عدد العمليات الفاشلة
```

#### **التصدير:**
```
📥 تصدير CSV - ملف CSV كامل بجميع السجلات المصفاة
- يشمل: التاريخ، المستخدم، النوع، التفاصيل، الحالة
- جاهز للتدقيق الإداري والمحاسبي
```

#### **الأمان:**
```
🔐 هذا السجل:
- لا يمكن تعديله
- لا يمكن حذفه
- محمي بالكامل
- للقراءة فقط
```

---

### **4️⃣ النبض الإداري اليومي (Daily Pulse Dashboard)**

#### **الوظيفة:**
```
موجز شامل تلقائي يُعرض للمدير العام عند الدخول - ملخص آخر 24 ساعة
```

#### **الإحصائيات الستة:**

##### **1. الجلسات المفتوحة حالياً 🟢**
```
- عدد الجلسات النشطة الآن
- لون أخضر
- أيقونة: Users
```

##### **2. القسم الأكثر نشاطاً 📊**
```
- اسم القسم الذي يتم استخدامه أكثر
- لون ذهبي
- أيقونة: TrendingUp
```

##### **3. التعديلات خلال 24 ساعة ✏️**
```
- عدد عمليات التعديل/التحديث
- لون أزرق
- أيقونة: CheckCircle
```

##### **4. العمليات الحساسة ⚠️**
```
- عدد العمليات الحرجة (حذف، تجميد، إنهاء)
- لون أحمر (إذا موجودة) / رمادي (إذا صفر)
- أيقونة: AlertTriangle
```

##### **5. عمليات تسجيل الدخول 🔑**
```
- عدد مرات تسجيل الدخول
- لون بنفسجي
- أيقونة: Clock
```

##### **6. إجمالي الإجراءات 📈**
```
- مجموع جميع الإجراءات الإدارية
- لون وردي
- أيقونة: Activity
```

#### **قسم العمليات الحساسة:**
```
⚠️ إذا كان هناك عمليات حذف/تجميد/إنهاء:

يظهر قسم خاص بعنوان:
"⚠️ عمليات حساسة حديثة"

يعرض آخر 5 عمليات حرجة مع:
- اسم المنفذ
- التفاصيل
- التوقيت
```

---

### **5️⃣ إدارة الصلاحيات التفصيلية**

#### **مدمجة مسبقاً:**
```
✅ المكون: AdvancedPermissionsManager
✅ الموقع: src/modules/admin/components/
✅ الوظيفة: إدارة دقيقة لصلاحيات كل مستخدم

المميزات:
- بطاقات مستخدمين تفاعلية
- صلاحيات لكل قسم:
  ✓ العرض (View)
  ✓ الإنشاء (Create)
  ✓ التعديل (Edit)
  ✓ الحذف (Delete)
- تحديث فوري بدون إعادة تحميل
- تجميد/تفعيل شامل
```

---

## 🔗 **الربط الكامل مع لوحة الإدارة (Events Bridge)**

### **كيف يعمل النظام:**

```typescript
// كل إجراء في لوحة الإدارة يُرسل حدث تلقائياً

// مثال 1: عند الدخول
AdminSessionService.createSession()
  → يُضاف إلى الجلسات الحية
  → يُسجل في Action Logs كـ "login"
  → يُحدث النبض الإداري

// مثال 2: عند التعديل في أي قسم
anyModule.update()
  → يُسجل في Action Logs كـ "update"
  → يُحدث عداد التعديلات في النبض الإداري

// مثال 3: عند الخروج
AdminSessionService.terminateSession()
  → يُغلق من الجلسات الحية
  → يُسجل في Action Logs كـ "logout"

// مثال 4: عند الحذف
anyModule.delete()
  → يُسجل في Action Logs كـ "delete"
  → يُضاف للعمليات الحساسة
  → إشعار فوري للمدير
```

### **جدول الأحداث:**

| الحدث | تأثير الرقابة | السجل | الإشعار |
|------|---------------|-------|----------|
| تسجيل دخول | ✅ جلسة جديدة | ✅ login | - |
| خروج | ❌ إغلاق جلسة | ✅ logout | - |
| تعديل صلاحيات | - | ✅ update_permissions | ✅ نعم |
| حذف | - | ✅ delete | ✅ نعم |
| تجميد | ⚠️ تحديث حالة | ✅ freeze_user | ✅ نعم |
| استكشاف المنصة | 🌍 وضع مؤقت | ✅ explore_platform | - |
| إنهاء جلسة | ❌ إغلاق فوري | ✅ session_terminated | - |

---

## 🎨 **الهوية البصرية الموحدة**

### **الألوان:**
```typescript
const controlPalette = {
  background: 'linear-gradient(135deg, #2E2A26 0%, #3D5B4B 100%)',
  gold: '#D4AF37',
  green: '#10B981',
  red: '#EF4444',
  blue: '#3B82F6',
  purple: '#A855F7',
  pink: '#EC4899',
  gray: '#6B7280',
  yellow: '#F59E0B',
};
```

### **البطاقات:**
```css
.control-card {
  background: linear-gradient(135deg, rgba(255,255,255,0.1) 0%, rgba(255,255,255,0.05) 100%);
  border: 2px solid [dynamic-color];
  box-shadow: 0 8px 32px [dynamic-color-20%];
  border-radius: 16px;
  transition: all 0.3s ease;
}

.control-card:hover {
  transform: scale(1.02) translateY(-2px);
}
```

### **الخطوط:**
```
- العناوين: Tajawal Black
- النصوص: Tajawal Bold/Medium
- الأرقام: Tajawal Black (كبيرة)
```

---

## 🧠 **الذكاء الإجرائي**

### **1. الكشف التلقائي عن الأنماط غير الطبيعية:**

```typescript
// النظام يراقب:
- حذف متكرر في فترة قصيرة (>5 خلال ساعة)
- تعديلات كثيرة على نفس البيانات
- محاولات دخول فاشلة متكررة
- جلسات طويلة جداً (>8 ساعات)

// عند الكشف:
⚠️ إشعار أمني فوري:
"نشاط إداري غير اعتيادي تم رصده"
```

### **2. التنبيهات الذكية:**

```typescript
// أنواع التنبيهات:

🔴 حرج (Critical):
- حذف مستخدم
- تجميد مستخدم
- إنهاء جلسات متعددة

🟡 متابعة (Warning):
- جلسة خاملة >20 دقيقة
- تعديلات متكررة
- تغيير صلاحيات

🟢 إعلام (Info):
- تسجيل دخول جديد
- استكشاف المنصة
- خروج عادي
```

### **3. التكامل المستقبلي (WhatsApp):**

```
📱 إرسال إشعارات واتساب للمدير العام عند:
- العمليات الحرجة
- الأنشطة غير الطبيعية
- تحديثات الصلاحيات المهمة
```

---

## 📊 **لوحة الرقابة الرئيسية (الواجهة الموحدة)**

### **التصميم المقترح:**

```
┌─────────────────────────────────────────────────────────────┐
│  🛡️ مركز الرقابة والصلاحيات                                 │
│                                                             │
│  [👥 المستخدمون]  [📊 الجلسات الحية]  [📝 السجل]  [⚡ النبض]  │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │                  المحتوى الديناميكي                   │   │
│  │         (حسب القسم المختار من الأعلى)              │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔧 **الاستخدام التقني**

### **استيراد المكونات:**

```typescript
import { AdminUsersManager } from './modules/admin/components/AdminUsersManager';
import { AdvancedLiveSessionsMonitor } from './modules/admin/components/AdvancedLiveSessionsMonitor';
import { ActionLogsViewer } from './modules/admin/components/ActionLogsViewer';
import { DailyPulseDashboard } from './modules/admin/components/DailyPulseDashboard';
import { AdvancedPermissionsManager } from './modules/admin/components/AdvancedPermissionsManager';
```

### **إضافة سجل حدث:**

```typescript
// في أي مكان بالتطبيق
await AdminSessionService.addAccessLog(
  phone,           // رقم الجوال
  name,            // اسم المستخدم
  actionType,      // نوع الإجراء
  details,         // التفاصيل
  status           // success أو failed
);
```

---

## ✅ **قائمة التحقق النهائية**

```
[✓] المستخدمون الإداريون - كامل
[✓] الجلسات الحية - مع تحديث كل 10 ثوان
[✓] سجل الإجراءات - غير قابل للتعديل/الحذف
[✓] النبض الإداري - ملخص 24 ساعة
[✓] إدارة الصلاحيات - تفصيلية ودقيقة
[✓] التكامل مع جميع الأقسام
[✓] تصدير CSV للسجلات
[✓] البحث والتصفية المتقدمة
[✓] التصميم الفاخر الموحد
[✓] البناء الناجح
```

---

## 📈 **الإحصائيات:**

```
✅ عدد المكونات: 5
✅ عدد الوظائف: 20+
✅ عدد أنواع الإجراءات المسجلة: 15+
✅ التحديث التلقائي: كل 10 ثوان
✅ الكود الجديد: ~2500 سطر
✅ التوثيق: شامل ومفصل
✅ الأمان: عالي جداً
```

---

**🎊 نظام رقابة وصلاحيات متكامل جاهز للإنتاج! 🚀✨**

**تاريخ آخر تحديث:** 2025-10-23
