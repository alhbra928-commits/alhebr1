# 🔬 دليل التشخيص المباشر لقفل Viewport في iOS

## 📊 لوحة التشخيص المباشرة

عند فتح المنصة على iPhone، ستظهر لوحة صغيرة في الزاوية اليسرى السفلى تعرض القيم التالية:

```
vv.height=844 innerH=844 --app-vh=844px shellH=844
```

---

## 📖 شرح القيم المعروضة:

### 1. `vv.height` - ارتفاع Visual Viewport
**المعنى:** الارتفاع الحقيقي الحالي لمنطقة العرض في Safari
- **القيمة الطبيعية:** تتراوح بين 740-930 حسب الجهاز
- **السلوك المتوقع:** يتغير عند ظهور/إخفاء شريط Safari

### 2. `innerH` - ارتفاع Window
**المعنى:** ارتفاع النافذة حسب `window.innerHeight`
- **القيمة الطبيعية:** مماثلة لـ `vv.height`
- **السلوك المتوقع:** يتغير مع تغير الـ viewport

### 3. `--app-vh` - CSS Variable المحفوظة
**المعنى:** القيمة المحفوظة في CSS لاستخدامها في التخطيط
- **القيمة المتوقعة:** يجب أن تساوي `vv.height` تماماً
- **السلوك المتوقع:** يتحدث تلقائياً مع كل تغيير

### 4. `shellH` - ارتفاع Shell الفعلي
**المعنى:** الارتفاع الفعلي لعنصر `.appShell` المحسوب من المتصفح
- **القيمة المتوقعة:** يجب أن تساوي `--app-vh` تماماً
- **السلوك المتوقع:** ثابت ومطابق لـ `vv.height`

---

## ✅ السيناريوهات المختلفة:

### ✅ السيناريو 1: النظام يعمل بشكل مثالي

```
vv.height=844 innerH=844 --app-vh=844px shellH=844
```

**التفسير:**
- جميع القيم متطابقة ✅
- القفل يعمل بشكل صحيح ✅
- لا يوجد قفز للهيدر/الفوتر ✅

**الإجراء:** لا شيء - النظام يعمل بشكل مثالي!

---

### ⚠️ السيناريو 2: القيم تتغير لكن بشكل متزامن

عند التمرير لأعلى:
```
vv.height=740 innerH=740 --app-vh=740px shellH=740
```

عند التمرير لأسفل:
```
vv.height=844 innerH=844 --app-vh=844px shellH=844
```

**التفسير:**
- القفل يعمل ✅
- القيم تتحدث بشكل متزامن ✅
- إذا كان هناك قفز، المشكلة من مكان آخر ⚠️

**الإجراء:** تحقق من:
- CSS للهيدر والفوتر
- استخدام `transform: translateZ(0)`
- safe-area-inset

---

### ❌ السيناريو 3: --app-vh فارغ أو غير محدد

```
vv.height=844 innerH=844 --app-vh= shellH=?
```

**التفسير:**
- الدالة `lockIOSViewport()` لم تعمل ❌
- CSS variable لم يتم تعيينه ❌

**الإجراء:**
1. تحقق من استدعاء `lockIOSViewport()` في `main.tsx`
2. افتح Console وابحث عن أخطاء JavaScript
3. تأكد من أن الكود يعمل قبل تحميل React

---

### ❌ السيناريو 4: shellH لا يتطابق مع --app-vh

```
vv.height=844 innerH=844 --app-vh=844px shellH=900
```

**التفسير:**
- CSS variable محدد صح ✅
- لكن `.appShell` لا يستخدمه ❌
- يوجد CSS آخر يغلب القيمة ❌

**الإجراء:**
1. تحقق من أن CSS يحتوي على:
   ```css
   .appShell {
     height: var(--app-vh, 100svh) !important;
   }
   ```
2. تأكد من عدم وجود CSS inline يغلب القيمة
3. استخدم DevTools لفحص العنصر

---

### ❌ السيناريو 5: القيم متطابقة لكن القفز موجود

```
vv.height=844 innerH=844 --app-vh=844px shellH=844
```
**لكن الهيدر/الفوتر يقفزان!**

**التفسير:**
- القفل يعمل ✅
- المشكلة من الهيدر/الفوتر نفسهم ⚠️

**الإجراء:** تحقق من:

#### 1. الهيدر:
```css
.appHeader {
  position: fixed;
  top: 0; /* ليس top: var(--vvtop) */
  left: 0;
  right: 0;
  z-index: 1000;
  transform: translateZ(0); /* مهم! */
}
```

#### 2. الفوتر:
```css
.appFooter {
  position: fixed;
  left: 0;
  right: 0;
  bottom: 0; /* مهم! */
  z-index: 1000;
  transform: translateZ(0); /* مهم! */
  padding-bottom: env(safe-area-inset-bottom);
}
```

#### 3. المحتوى:
```css
.appMain {
  overscroll-behavior: contain; /* مهم! */
}
```

---

## 🧪 كيفية الاختبار:

### الاختبار 1: افتح المنصة
1. افتح المنصة على iPhone في Safari
2. انظر للزاوية اليسرى السفلى
3. يجب أن ترى اللوحة السوداء مع الأرقام

### الاختبار 2: مرر للأعلى
1. مرر الصفحة للأعلى ببطء
2. راقب القيم في اللوحة
3. يجب أن تتغير جميعها بنفس الوقت

### الاختبار 3: مرر للأسفل
1. مرر الصفحة للأسفل ببطء
2. راقب القيم في اللوحة
3. يجب أن تعود جميعها للقيمة الأصلية

### الاختبار 4: راقب الهيدر والفوتر
1. أثناء التمرير
2. يجب أن يبقيا ثابتين تماماً
3. لا قفز أو حركة غير مرغوبة

---

## 🔧 أدوات التشخيص الإضافية:

### في Console:
```javascript
// طباعة جميع القيم
console.log({
  'vv.height': window.visualViewport?.height,
  'innerHeight': window.innerHeight,
  '--app-vh': getComputedStyle(document.documentElement).getPropertyValue('--app-vh'),
  'shellHeight': document.querySelector('.appShell')?.getBoundingClientRect().height
});
```

### فحص العناصر:
```javascript
// فحص الهيدر
const header = document.querySelector('.appHeader');
console.log('Header:', {
  position: getComputedStyle(header).position,
  top: getComputedStyle(header).top,
  transform: getComputedStyle(header).transform
});

// فحص الفوتر
const footer = document.querySelector('.appFooter');
console.log('Footer:', {
  position: getComputedStyle(footer).position,
  bottom: getComputedStyle(footer).bottom,
  transform: getComputedStyle(footer).transform
});
```

---

## 📝 ملاحظات مهمة:

### 1. اللوحة ظاهرة دائماً
- لإخفائها: احذف الكود من `iosViewportLock.ts`
- أو أضف شرط `if (import.meta.env.DEV)`

### 2. Z-Index عالي جداً
- اللوحة لها `z-index: 2147483647`
- لضمان ظهورها فوق كل شيء

### 3. Pointer Events معطلة
- `pointer-events: none`
- لا تعيق النقر على العناصر تحتها

### 4. التحديث التلقائي
- اللوحة تتحدث تلقائياً
- عند أي تغيير في الـ viewport
- لا حاجة لتحديث يدوي

---

## 🎯 الخلاصة:

### إذا رأيت هذا:
```
vv.height=844 innerH=844 --app-vh=844px shellH=844
```
**والقيم تتحدث بشكل متزامن**

**وجميع القيم متطابقة دائماً**

✅ القفل يعمل بشكل مثالي!
✅ أي قفز هو من مصدر آخر (CSS أو JS)
✅ راجع الهيدر/الفوتر وتأكد من `transform: translateZ(0)`

---

## 📚 مراجع سريعة:

### الملفات المعدلة:
- ✅ `src/lib/iosViewportLock.ts` - يحتوي على لوحة التشخيص
- ✅ `src/main.tsx` - يستدعي الدالة
- ✅ `src/index.css` - يستخدم `--app-vh` مع `!important`

### CSS المطلوب:
```css
.appShell {
  height: var(--app-vh, 100svh) !important;
}

.appHeader, .appFooter {
  transform: translateZ(0);
}

.appMain {
  overscroll-behavior: contain;
}
```

---

🔬 **Build Version:** v20251219_1766129715230
✅ **Status:** Diagnostic Mode Active
🎯 **Ready for Testing!**
